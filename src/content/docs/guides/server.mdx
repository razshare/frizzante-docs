---
title: Start server
---

Use `servers.New().Start()` to create and start a new server.

```go
//main.go
package main

import "github.com/razshare/frizzante/servers"

func main() {
    servers.New().Start()
}
```