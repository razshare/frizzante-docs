---Route
title: Start server
---

Use `servers.New().Start()` to create and start a server.

```go
//main.go
package main

import "github.com/razshare/frizzante/web"

func main() {
    servers.New().Start()
}
```