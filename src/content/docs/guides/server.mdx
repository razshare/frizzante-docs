---
title: Start server
---

Use `NewServer().Start()` to create and start a new server.

```go
//main.go
package main

import "github.com/razshare/frizzante/frz"

func main() {
    frz.NewServer().Start()
}
```