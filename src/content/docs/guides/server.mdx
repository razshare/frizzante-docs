---
title: Start server
---

Use `web.NewServer().Start()` to create and start a server.

```go
//main.go
package main

import "github.com/razshare/frizzante/web"

func main() {
    web.NewServer().Start()
}
```