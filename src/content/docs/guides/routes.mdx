---
title: Routes
---

import { Tabs, TabItem, FileTree } from '@astrojs/starlight/components';

You can add a new route to the server with `AddRoute()`.

```go
//main.go
package main

import (
    "github.com/razshare/frizzante/libsrv"
    "main/lib/handlers"
)

var server = libsrv.NewServer()

var hello = libsrv.Route{
    Pattern: "GET /", 
    Handler: handlers.Hello,
}

func main() {
    server.AddRoute(hello) // Adds route.
    server.Start()
}
```

```go
//lib/handlers/hello.go
package handlers

import "github.com/razshare/frizzante/libcon"

func Hello(con *libcon.Connection) {
    con.SendMessage("Hello") // Sends text.
}
```